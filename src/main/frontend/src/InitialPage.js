import React from "react";
import { Typography } from "@mui/material";
import {Layout} from "./layout/Layout";

export const InitialPage = () => (
  <Layout>
    <Typography variant={"h4"}>Greeting Professor Falken!</Typography>
  </Layout>
);
